import { InjectMetaInfoInterceptor } from './inject-meta-info.interceptor';

describe('InjectMetaInfoInterceptor', () => {
	it('should be defined', () => {
		expect(new InjectMetaInfoInterceptor()).toBeDefined();
	});
});
